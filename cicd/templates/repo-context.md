# Repo context for CICD judges

> Delete this line and replace with a short, dense description of this
> repository. CICD passes this into every LLM judge so the judges evaluate
> changes against what *your* repo actually does, not generic best-practice.

## What this repo is

- One-sentence purpose:
- Primary language & runtime:
- Public surface (CLI, service, library, docs-only):

## Conventions this repo expects

- Branching model:
- Testing expectations:
- Docs location and scope:

## Hazards / footguns

- Hot paths that are fragile:
- Things agents have historically broken:
- Secrets / credentials present in this repo (should be zero):
