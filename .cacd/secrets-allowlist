# .cacd/secrets-allowlist — regex patterns (one per line) whose matches are ignored
# by the secrets gate. Lines beginning with '#' are comments.
#
# The gate applies these AFTER pattern detection. Use sparingly; each entry
# should be narrow and auditable.

# AWS "EXAMPLE" key literal baked into test fixtures on purpose.
AKIAIOSFODNN7EXAMPLE
