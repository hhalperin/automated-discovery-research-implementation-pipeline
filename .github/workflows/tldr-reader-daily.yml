# TLDR Reader — Ultra-Lean Digest
# Runs daily at 7:00 AM UTC. Uses Cursor Cloud Agents API to launch the agent.
# Alternative to creating an automation at cursor.com/automations/new (no API for that).

name: TLDR Reader Daily

on:
  schedule:
    # 7:00 AM UTC daily. Adjust for local: 7 AM EST = 12 UTC, 7 AM PST = 15 UTC
    - cron: "0 7 * * *"
  workflow_dispatch:  # Allow manual trigger

jobs:
  launch-tldr-reader:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Launch TLDR Reader agent
        id: launch
        env:
          # GitHub Actions only sees secrets configured on the repo/org — not Cursor IDE user settings.
          # Accept common names so one secret can match dashboard or docs wording.
          CURSOR_API_KEY: ${{ secrets.CURSOR_AGENTS_API_KEY || secrets.CURSOR_API_KEY || secrets.CURSOR_CLOUD_AGENTS_API_KEY }}
          REPO_URL: ${{ github.server_url }}/${{ github.repository }}
        run: |
          if [ -z "$CURSOR_API_KEY" ]; then
            echo "::error::No Cursor Cloud Agents API key in GitHub Actions secrets."
            echo "::error::Add one of: CURSOR_AGENTS_API_KEY, CURSOR_API_KEY, or CURSOR_CLOUD_AGENTS_API_KEY"
            echo "::error::at Repository Settings → Secrets and variables → Actions (organization secrets also work)."
            echo "::error::IDE-only env vars (Cursor user settings) are not visible to this workflow."
            exit 1
          fi

          TODAY=$(date -u +%Y-%m-%d)
          PROMPT_CONTENT=$(sed "s/{YYYY-MM-DD}/$TODAY/g" .cursor/automations/tldr-reader-ultra-lean/prompt.md)
          BODY=$(jq -n \
            --arg prompt "$PROMPT_CONTENT" \
            --arg repo "$REPO_URL" \
            '{prompt: {text: $prompt}, source: {repository: $repo, ref: "main"}, target: {autoCreatePr: false}}')

          echo "Launching agent for $TODAY..."
          RESPONSE=$(curl -s -w "\n%{http_code}" -X POST "https://api.cursor.com/v0/agents" \
            -H "Authorization: Bearer $CURSOR_API_KEY" \
            -H "Content-Type: application/json" \
            -d "$BODY")

          HTTP_CODE=$(echo "$RESPONSE" | tail -n1)
          BODY_RESP=$(echo "$RESPONSE" | sed '$d')

          if [ "$HTTP_CODE" -ge 200 ] && [ "$HTTP_CODE" -lt 300 ]; then
            AGENT_ID=$(echo "$BODY_RESP" | jq -r '.id')
            echo "Agent launched: $AGENT_ID"
          else
            echo "::error::API request failed (HTTP $HTTP_CODE)"
            echo "$BODY_RESP" | jq . 2>/dev/null || echo "$BODY_RESP"
            exit 1
          fi
